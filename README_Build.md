
for install and build
```bash
$ cd <pasta do projeto>
$ [sudo] npm install 
```

for run
```bash
$ cd <pasta do projeto>
$ sudo node .\main.js <http-port> <telnet-port> [-h <telnet-host>] [-w <path/to/www>] ［－c <charset>]
```